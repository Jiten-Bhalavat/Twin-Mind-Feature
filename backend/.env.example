# Copy this to .env and fill in values if needed
# The Groq API key is user-provided at runtime via the frontend modal
# No keys should be hardcoded or committed

# Optional: set host/port for local dev
HOST=0.0.0.0
PORT=8000
